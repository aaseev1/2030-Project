const Review = (title, review, rating, userId, username, postedAt) => {
    return {
      title,
      review,
      rating,
      userId,
      username,
      postedAt
    };
  };
  
module.exports = Review;
  